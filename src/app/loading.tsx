export default function AppLoading() {
  // Intentionally empty to avoid overriding nested segment loaders.
  // We provide loaders in specific segments (e.g., dashboard, practice) to keep headers visible.
  return null
}


